# Star Wars Repo

! [./starWars.jpg](Uma mensagem para a foto)

Este repositório é um treinamento de Git.

git revert --no-commit <commit-id> -n HEAD

se aparecer (master|REVERTING), escolher uma das opções após o revert:

git revert --continue / --quit / --abort